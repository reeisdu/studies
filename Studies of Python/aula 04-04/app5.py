# Activitie 5: Using the RANGE;
# Now we´re doing the same thing, but more quickly. We go directly for the "for (x) in RANGE". 
# the range is used only for number.

for i in range(100):
    print(i, end = '')


add = 0
for i in range(10):
    num = float(input('Write a Real Number: '))
    add = add + num

print('The result of the sum is: ', add)

